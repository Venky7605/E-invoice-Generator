{
  "name": "gepp-tool",
  "version": "2.0.0",
  "description": "GePP Tool - GST e-Invoice Management",
  "scripts": {
    "build": "pip install --break-system-packages -r requirements.txt || pip install --user -r requirements.txt",
    "start": "python -m streamlit run app.py --server.port ${PORT:-8501} --server.address 0.0.0.0 --server.headless true"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
